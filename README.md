## Getting started

to use this package you need to use following code in your project
```code
            EditableTextWidget(
                initialText: "text",
                onTextChangedSubmitted: (newText) {
                 print(newText)
                })
```
